API_KEY?=""
GOOGLE_APPLICATION_CREDENTIALS?=""
FIREBASE_AUTH_EMULATOR_HOST="localhost:9099"

tidy:
	go mod tidy

clean:
	rm -rf ./bin Gopkg.lock

test:
	PROJECT_ID=demo-project FIREBASE_AUTH_EMULATOR_HOST=localhost:9099 API_KEY=API_KEY FIRESTORE_EMULATOR_HOST=localhost:8090 GOOGLE_APPLICATION_CREDENTIALS=GOOGLE_APPLICATION_CREDENTIALS go test ./...
